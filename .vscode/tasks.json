{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "func",
			"label": "func: host start",
			"command": "host start --port 8000",
			"problemMatcher": "$func-python-watch",
			"isBackground": true,
			"dependsOn": "uv sync",
			"options": {
				"cwd": "${workspaceFolder}"
			}
		},
		{
			"label": "uv sync",
			"type": "shell",
			"osx": {
				"command": "uv sync && uv pip freeze > requirements.txt"
			},
			"windows": {
				"command": "uv sync && uv pip freeze > requirements.txt"
			},
			"linux": {
				"command": "uv sync && uv pip freeze > requirements.txt"
			},
			"problemMatcher": [],
			"options": {
				"cwd": "${workspaceFolder}"
			}
		},
		{
			"label": "prepare deployment",
			"detail": "Not used, was used to install problematic dependencies locally to avoid issues during deployment to Azure.",
			"type": "shell",
			"dependsOn": "uv sync",
			"command": "pip install -r requirements.txt --target .python_packages/lib/site-packages --no-user",
			"problemMatcher": [],
			"options": {
				"cwd": "${workspaceFolder}"
			}
		}
	]
}